import { StyleSheet } from 'react-native';

const StylesStart = StyleSheet.create({
    background: {
        flex: 1,
        resizeMode: 'cover',
    },
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        padding: 20,
    },
    chartContainer: {
        alignItems: "center", // Centraliza o gráfico na tela
        justifyContent: "center",
        marginTop: 20, // Espaço superior
        padding: 10,
      },
  });

  export default StylesStart;